# Только для тестов. Программе они не нужны, и ставить их пользователю незачем:
# один playwright с браузером -- это сотни мегабайт сверх самой программы.
#
#     .venv/Scripts/python -m pip install -r requirements-dev.txt
#     .venv/Scripts/python -m playwright install chromium

-r requirements.txt

# TestClient ходит по сети через клиент, который выбирает starlette, а он с
# версии 1.6 просит httpx2: «import httpx2 as httpx», и только если не вышло --
# старый httpx с предупреждением об устаревании.
#
# В версии 1.14.2 я принял httpx2 за опечатку и заменил его на httpx. Это была
# ошибка: пакет настоящий и нужен именно он. Здесь перечислены оба -- httpx
# остаётся на случай, если попадётся starlette постарше.
httpx2>=2.12
httpx>=0.27

# Разбор кода веб-интерфейса: проверки сверяют разметку с обработчиками.
esprima>=4.0

# Браузерные сценарии.
playwright>=1.40
