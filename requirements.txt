fastapi>=0.115
uvicorn[standard]>=0.30
requests>=2.32
pydantic>=2.7
cryptography>=43.0

# Сканер карт камерой: Pillow читает кадр, numpy считает отпечаток.
# Программа запускается и без них -- не работает только сканер.
Pillow>=10.0
numpy>=1.24

# Поиск карты в кадре: с ним карту можно класть под камеру как угодно, без
# него её приходится совмещать с рамкой на экране. Тяжёлый пакет (~110 МБ),
# но только он превращает сканер в то, чем им хочется пользоваться.
opencv-python-headless>=4.9

# Разбор снимка пачки: имена читает распознаватель самой Windows, а этот
# пакет -- только доступ к нему. Моделей не качает, места не занимает.
#
# Версия указана с «b10» не для красоты: на PyPI у winsdk выпущены одни
# пре-релизы, а pip под «>=1.0.0» пре-релизы не берёт -- и вся установка
# обрывалась на этой строке, из-за чего программа не запускалась ни на одной
# чистой машине.
winsdk>=1.0.0b10; sys_platform == "win32"
